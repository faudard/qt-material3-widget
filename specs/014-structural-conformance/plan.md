# Implementation Plan

## Phase 014.A — first static vertical

1. Consume the separate 10-case Filled Button structure suite.
2. Extract active `ButtonSpec` values without compiling Qt.
3. Validate public registry and resolver bindings.
4. Validate token scopes and required render-slot evidence.
5. Reject conflicts and drift with no suppression mechanism.
6. Add negative tests and the repository-health gate.

The comparator reports only the named fixture; it does not imply full Filled
Button or Buttons-family conformance.
