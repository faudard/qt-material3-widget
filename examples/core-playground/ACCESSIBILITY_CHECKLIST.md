# Accessibility validation checklist

- Tab order is deterministic across visible controls
- Shift+Tab returns correctly
- Space activates checkable / button controls
- Enter activates default button behavior where expected
- Focus remains visible on all keyboard-focusable widgets
- Icon-only controls expose an accessible name
- Placeholder-only inputs are reviewed for accessible labeling
