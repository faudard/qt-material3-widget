# Coding rules

- Public headers contain declarations only.
- Private headers never leak into `include/`.
- Widgets render from specs.
- No raw string-key theme lookups in new code.
- No hardcoded design metrics in final widgets.
