#pragma once

// QtMaterialFilledTextField currently reuses the shared text-field shell helper
// through QtMaterialOutlinedTextField. Keep this private header as the place to
// introduce filled-variant-only private state later if the variants diverge.
